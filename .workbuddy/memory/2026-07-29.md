# 2026-07-29

## 站点访问与资源路径规范化
- 确认主页面 `gwm_web_index.html` 存在（29650 字节）。
- 根目录 `index.html` 已存在且正确：meta 跳转 + `location.href="./gwm_web_index.html"`，含手动跳转链接，使用相对路径。按"如有请忽略"保留。
- `.nojekyll` 已存在（空文件），保留用于 GitHub Pages 静态资源放行。
- 新建 `.gitignore`，内容：`*.log` / `.DS_Store` / `.vscode` / `*.tmp`。
- 全量扫描所有 html/css/js：无任何 `/`-前缀或绝对文件路径资源引用；所有页面 `<base href="./">` 均为相对路径；css/style.css(30KB)、js/app.js(45KB) 经本地 http 服务验证可正常访问。
- 启动本地静态服务：`python -m http.server 8099 --bind 127.0.0.1`（后台 task y48K3g）。验证 `/` 返回 312B 跳转页，`/gwm_web_index.html` 返回 200 且 29650B 正文。
